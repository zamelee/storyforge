/**
 * 共享提示词片段（用于打破 prompt-seeds / prompt-seeds-genre-packs / prompt-seeds-genre-packs-extended 之间的循环依赖）。
 *
 * 这里只放 userPromptTemplate 里用到的模板片段（字符串），不放任何种子对象。
 */

/** 铁律·角色绑定。强制 AI 从项目角色库选取主角/配角/反派,严禁编造新人名/新势力名作为核心角色。详见 userPromptTemplate 里的引用。 */
export const OUTLINE_CHARACTER_BINDING = `【铁律·角色绑定】本作品已建有完整角色库（见上文「已创建的角色」）。推动主线剧情的主角、配角、反派、对手、关键关系人，**必须从角色库中选取或由角色库中的人物派生**；严禁凭空捏造全新人名、新势力名作为核心角色。

- 主角选取：若角色库中有标记为「main」或「主角」的角色，必须直接以该角色为主角，不得用其他名字替代。
- 配角使用：推动卷/章节核心冲突的配角必须来自角色库；库中已有的角色优先复用，不要重名复制（如同名同姓的角色不要重复造）。
- 反派/对手：使用角色库中已有的反派或「道德轴 evil」类角色；只有当角色库完全为空时，才允许新创反派但需明确标注为「路人/背景板」。
- 路人/背景板：可临时起名，但不得与角色库已有名字撞名，且不得在后续卷/章节升级为主线角色。
- 地名/势力名/组织名等设定元素同理——世界观已建立的世界要素不得被同义替换或重新发明（如角色库所在世界观已有「衡瑾律所」则必须沿用，不得另造「正诚/明远」等替代名）。

若用户/作品世界观与角色库存在明显矛盾（例如世界观要求主角是法师但角色库主角是现代人），请在回答开头明确指出矛盾并优先尊重用户最新补充说明。`
